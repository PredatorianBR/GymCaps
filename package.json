{
  "name": "gymcaps",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve -p 3000",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp public/_redirects dist/_redirects",
    "preview": "npx serve -p 3000 dist",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting needed'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
